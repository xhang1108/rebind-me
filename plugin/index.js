// Rebind Me opencode plugin.
//
// Placeholder entry. The real plugin forwards session events to the local
// bridge for status lighting and reports terminal identity via /api/sessions.
// Implemented in stage 6 with TypeScript and @opencode-ai/plugin types.

/** Marker used by the CI syntax check. */
export const name = "@xhang98/rebind-me";

export default function RebindMe() {
  return {};
}
